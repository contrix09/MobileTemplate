<?xml version="1.0" encoding="utf-8" ?>
<views:BaseView xmlns="http://xamarin.com/schemas/2014/forms"
                xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                xmlns:views="clr-namespace:MobileTemplate.Views"
                xmlns:viewmodels="clr-namespace:MobileTemplate.ViewModels"
                x:Class="MobileTemplate.Views.MainView"
                x:TypeArguments="viewmodels:MainViewModel">
    <StackLayout>
        <!-- Place new controls here -->
        <Label Text="{Binding WelcomeText}"
               HorizontalOptions="Center"
               VerticalOptions="CenterAndExpand">
            <Label.GestureRecognizers>
                <TapGestureRecognizer Command="{Binding LabelTapCommand}"
                                      CommandParameter="Starting Task"
                                      NumberOfTapsRequired="1" />
            </Label.GestureRecognizers>
        </Label>
    </StackLayout>
</views:BaseView>
