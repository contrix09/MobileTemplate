<?xml version="1.0" encoding="utf-8" ?>
<views:BaseMainView x:Class="MobileTemplate.Views.MainView"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:MobileTemplate.Views"
    BackgroundColor="{StaticResource Color.Background}">
    <views:BaseMainView.Content>
        <StackLayout VerticalOptions="Center">
            <Label x:Name="WelcomeLabel"
                HorizontalOptions="Center"
                Style="{StaticResource Style.MainView.Title}"
                Text="{StaticResource String.AppTitle}"
                VerticalOptions="Center">
                <Label.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding LabelTapCommand}"
                        CommandParameter="Parameter"
                        NumberOfTapsRequired="1" />
                </Label.GestureRecognizers>
            </Label>
        </StackLayout>
    </views:BaseMainView.Content>
</views:BaseMainView>
